# CLAUDE.md — kira_zone

## Repository Overview

**Owner:** kiratsao
**Remote:** github.com/kiratsao/kira_zone
**Primary branch:** master

This repository is currently in its initial state with no application code, build system, or CI/CD pipeline. It contains only a README.md.

## Project Structure

```
kira_zone/
├── README.md          # Project title only
└── CLAUDE.md          # This file
```

No source code, dependencies, configuration files, or tests exist yet.

## Development Conventions

### Git

- **Default branch:** master
- **Commit style:** Short, imperative subject lines (e.g., "Add feature X", "Fix bug in Y")
- **Branch naming:** Use descriptive names with a prefix (e.g., `feature/`, `fix/`, `docs/`)

### When Adding Code

As this project grows, follow these guidelines:

- Choose a single language/framework and document the choice in this file
- Add a `.gitignore` appropriate for the chosen stack
- Include dependency lockfiles in version control
- Set up linting and formatting from the start
- Add a build/run section to this file once a stack is chosen
- Never commit secrets, `.env` files, or credentials

## Build & Run

No build system configured yet. Update this section when one is added.

## Testing

No test framework configured yet. Update this section when one is added.

## Common Tasks

_To be populated as the project develops._
